{% extends '::base.html.twig' %}

{% block title %}{% endblock %}

{% block container %}

    <div class="col-sm-12 col-md-6">
        <div class="caption">
            <h3>{{ school.name }}</h3>
            <h4>{{ school.number}}</h4>
            <p>{{ school.description }}</p>
            <p><a href="#" class="btn btn-primary" role="button">Сделать что-то</a> <a href="#" class="btn btn-default" role="button">... и еще что-то</a></p>
        </div>
    </div>

{% endblock %}